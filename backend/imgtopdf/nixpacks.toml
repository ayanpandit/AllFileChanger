[phases.setup]
nixPkgs = ['python311', 'gcc', 'libjpeg', 'zlib', 'libtiff', 'libwebp', 'libpng']

[phases.install]
cmds = ['pip install -r python/requirements.txt']

[phases.build]
cmds = ['echo "Build phase complete"']

[start]
cmd = 'cd python && gunicorn --config gunicorn.conf.py app:app'
