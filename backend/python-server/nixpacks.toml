# System packages required by the Python backend
[phases.setup]
nixPkgs = ["poppler_utils", "tesseract", "libreoffice"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:$PORT --workers 4 --timeout 120 --access-logfile -"
