# ============================================================================
# ALLFILECHANGER – Environment Variables
# ============================================================================
# Set these to your deployed backend URLs before building for production.
# Vite bakes these in at build time, so rebuild after changing.
# ============================================================================

# Node.js backend (image processing)
VITE_NODE_API_URL=http://localhost:5001

# Python backend (PDF & document processing)
VITE_PYTHON_API_URL=http://localhost:5050
