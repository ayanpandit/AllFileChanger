# ================== REACT ROUTER SPA REDIRECTS ==================
# This file fixes the "Not Found" issue when refreshing pages on Render

# Primary SPA fallback - redirects all routes to index.html for client-side routing
/*    /index.html   200

# Specific redirects for main routes (for better SEO)
/image-to-pdf/*         /index.html   200
/image-compressor/*     /index.html   200
/pdf-to-image/*         /index.html   200
/pdf-compressor/*       /index.html   200
/image-converter/*      /index.html   200

# Tool category redirects
/tools/*                /index.html   200
/pages/*                /index.html   200

# Common route patterns
/about/*                /index.html   200
/contact/*              /index.html   200
/privacy/*              /index.html   200
/terms/*                /index.html   200

# API routes should not be redirected (if you add backend API later)
/api/*                  /api/:splat   200

# Static assets should be served as-is
/assets/*               /assets/:splat   200
/images/*               /images/:splat   200
/favicon.ico            /favicon.ico     200
/robots.txt             /robots.txt      200
/sitemap.xml            /sitemap.xml     200
