# ================== RENDER SPA REDIRECTS ==================
# Specific format for Render static site hosting

# Static assets first - highest priority
/assets/*     /assets/:splat   200
/vite.svg     /vite.svg        200
/favicon.ico  /favicon.ico     200
/robots.txt   /robots.txt      200
/sitemap.xml  /sitemap.xml     200

# API routes should not be caught (if any)
/api/*        /api/:splat      404

# All other paths to React Router (SPA fallback)
/*            /index.html      200
